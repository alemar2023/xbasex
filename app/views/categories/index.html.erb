<p style="color:green"><%= notice %></p>
<h2>Categories</h2>
<div id="categories">
  <% @categories.each do |category| %>
  <%= render category %>
    <p><%= link_to "Show this category", category %></p>
  <% end %>
</div>

<%= link_to "New Category", new_category_path %>