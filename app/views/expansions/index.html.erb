<p style="color:green"><%= notice %></p>
<h2> Expansions</h2>
<div id="expansions">
  <% @expansions.each do |expansion| %>
  <%= render expansion %>
    <p>
      <%= link_to "show this expansion", expansion %>
    </p>
  <% end %>
</div>

<%= link_to "New Expansion", new_expansion_path %>